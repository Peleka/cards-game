

const initialState = {
};

export type InitialStateType = typeof initialState

export const reducer1 = (state: InitialStateType = initialState, action: any): InitialStateType => {
    return state
};